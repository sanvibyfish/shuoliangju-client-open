/** @format */

import Taro from '@tarojs/taro'
import modelExtend from 'dva-model-extend'
import {baseProduct} from '../base/base-product'
import * as Api from '../services/app'

export default modelExtend(baseProduct,{
  namespace: 'product',
  state: {
  },

  effects: {
  },

  reducers: {
  },
})
